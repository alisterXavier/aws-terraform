{
  "name": "jenkins-eks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild ./src/index --bundle --format=cjs --outfile=dist/index.js --platform=node",
    "build-docker": "./jenkins-eks/esbuild ./src/index --bundle --format=cjs --outfile=dist/index.js --platform=node",
    "start": "node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.20.2",
    "eslint": "^8.57.0",
    "express": "^4.19.2"
  }
}
